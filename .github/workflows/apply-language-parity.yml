name: Apply language parity migration

on:
  push:
    branches:
      - codex/fix-process-verbaal-language-parity
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Parity payload upload in progress"
